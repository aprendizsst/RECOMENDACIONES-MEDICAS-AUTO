# Backend Google Apps Script · V7

Archivos:

- `Code.gs`: usuarios, Gemini, consecutivos, recursos compartidos y correo.
- `BackendBridge.html`: puente compatible con la Web App.
- `appsscript.json`: scopes requeridos.

Después de reemplazar el código ejecuta manualmente `authorizePortalServices()` y luego comprueba `testGeminiPermission()` y `testGeminiApi()`. Publica una nueva versión de la Web App conservando la URL `/exec`.
